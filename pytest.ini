[pytest]
# Only collect from tests/. Without this, pytest tries to import
# head_detection/head_detection_test.py (it matches the default *_test.py
# pattern), which is a script, not a test module: importing it runs its work
# at module scope and creates output directories as a side effect.
testpaths = tests
python_files = test_*.py
