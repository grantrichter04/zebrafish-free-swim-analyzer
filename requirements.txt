# Zebrafish Free Swim Analyzer - Python dependencies
#
#   pip install -r requirements.txt
#
# PREFER `pip install -e ".[dev]"`. pyproject.toml is the source of truth and
# also installs the package itself, which is what lets `import fish_analyzer`
# work outside the repository root. This file carries the same constraints for
# anyone who wants dependencies only, and must be kept in step by hand.
#
# Requires Python >= 3.9. Verified working on 3.9, 3.12 and 3.13.
# See README.md for the conda alternative.

# --- Core (required) ---------------------------------------------------------
numpy>=1.24
pandas>=1.5
matplotlib>=3.7
scipy>=1.10
traja==25.0.1

# traja imports sklearn unconditionally but does not declare it as a
# dependency, so `import traja` fails without this. Not optional.
scikit-learn>=1.0

# --- Optional: spatial analysis (thigmotaxis) --------------------------------
# Without shapely, thigmotaxis and arena tools are disabled, not fatal.
shapely>=2.0

# --- Optional: video frame reading (Inspector tab) ---------------------------
# Without opencv, the frame inspector is disabled, not fatal.
opencv-python>=4.8

# --- Development only --------------------------------------------------------
# Not listed here, because it is not needed to run the application. Install the
# test suite's requirements with the dev extra instead:
#   pip install -e ".[dev]"

# --- Not installed here ------------------------------------------------------
# fish_posture_analyzer.py and head_detection/ are standalone scripts with
# heavier requirements the main package does not need:
#   scikit-image   (both)
#   idtrackerai    (head_detection/ only - install per idtracker.ai's own docs)
